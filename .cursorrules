1.每次有文件的更新和变动时,都需要采用git add . 和 git commit -m "更新内容" 和 git push 进行提交和推送。
2.git commit -m "更新内容" 的更新内容需要是中文,开头都是"Cursor-更新内容",这样我就好知道是怎么回事。
3.当任务完成后，你需要扮演一个任务检查者角色,你需要:
(1)检查任务都有哪些？
(2)每个任务的完成情况，然后针对任务进行复查和打分。
(3)如果任务没有完成，需要给出原因和解决方案。
(4)针对此次整体的任务情况，给出总结和建议。

